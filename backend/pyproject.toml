[project]
name = "adlint"
version = "0.1.0"
description = "Python backend for AdLint — GTM/GA4 audit engine"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
